<md-toolbar class="md-table-toolbar md-default" ng-hide="selected.length || filter.show">
  <div class="md-toolbar-tools">
    <h2 class="md-title">会员卡种类</h2>
    <div flex></div>
    <md-button  class="md-raised md-primary" ng-click="addItem($event)">
      新增卡种类
    </md-button>
  </div>
</md-toolbar>



<md-table-container>
  <table md-table md-row-select ng-model="selected" md-progress="promise">
    <thead md-head md-order="query.order" md-on-reorder="onReorder">
      <tr md-row>
        <th md-column md-order-by="nameToLower"><span>种类名称</span></th>
        <th md-column md-numeric md-order-by="type"><span>总金额（元）</span></th>
        <th md-column md-numeric>会员卡折扣</th>
        <th md-column md-numeric>月利息</th>
      </tr>
    </thead>
    <tbody md-body>
      <tr md-row md-select="cardType" md-select-id="{{cardType.name}}" md-auto-select ng-repeat="cardType in cardTypes.data">
        <td md-cell><a href="/app/indexModifyCardType.html?id={{cardType.name}}">{{cardType.name}}</a></td>
        <td md-cell>{{cardType.totalMoney}}</td>
        <td md-cell>{{cardType.discount}}</td>
        <td md-cell>{{cardType.interest}}</td>

      </tr>
    </tbody>
  </table>
</md-table-container>

<md-table-pagination md-limit="query.limit" md-page="query.page" md-total="{{cardTypes.count}}" md-on-paginate="onPaginate" md-page-select></md-table-pagination>
